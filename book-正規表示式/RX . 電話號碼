
寫一個名為 isPhoneNumber() 的函式,
用來檢查字串在比對時是否符合此模式,
然後返回 True 或 False.

def isPhoneNumber(text):
  if len(text) != 12:
    return False
  for i in range(0, 3):
    if not text[i].isdecimal():
      return False

  if text[3] != '-':
    return False
  for i in range(4, 7):
    if not text[i].isdecimal():
      return False
